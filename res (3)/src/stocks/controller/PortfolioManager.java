package stocks.controller;


/**
 * This interface represents the functionalities of our controller.
 * It acts as a mediator between the model and the view.
 */
public interface PortfolioManager {

  /**
   * A single runner function for our Portfolio manager.
   * It takes care of user inputs and acts as an orchestrator between the model and view.
   */
  void execute();

}